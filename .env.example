# Point this to your self-hosted Invidious instance to use it as the primary
# search, metadata, and progressive-stream provider. yt-dlp remains a fallback.
INVIDIOUS_URL=http://localhost:3001

# Optional but recommended: enables official channel artwork and metadata.
YOUTUBE_API_KEY=
